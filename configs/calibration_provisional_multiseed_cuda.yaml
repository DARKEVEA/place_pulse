extends: calibration_cuda.yaml

simulation:
  model_repetitions: 5
  model_mechanisms: ["null", "scalar", "continuous", "mixture"]
  repetitions: 5

reporting:
  artifacts_dir: artifacts/run_017_provisional_multiseed
  run_label: RUN_017_PROVISIONAL_MULTISEED
