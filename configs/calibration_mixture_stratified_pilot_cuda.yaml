extends: calibration_cuda.yaml

simulation:
  model_repetitions: 1
  model_mechanisms: ["mixture"]
  repetitions: 1

reporting:
  artifacts_dir: artifacts/run_015_mixture_stratified_pilot
  run_label: RUN_015_MIXTURE_STRATIFIED_PILOT
