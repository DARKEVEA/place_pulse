extends: ../replication_provisional_cuda.yaml

data:
  primary_dimension: depressing

reporting:
  artifacts_dir: artifacts/run_020_replication_dimensions/depressing_seed_1103
  run_label: RUN_020_REPLICATION_DEPRESSING_SEED_1103
