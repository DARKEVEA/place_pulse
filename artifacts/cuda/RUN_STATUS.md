# RUN_001_DIAGNOSTIC

The artifacts in this directory are retained as an immutable engineering and
diagnostic record. They are not confirmatory scientific evidence because the
M1 scalar model did not beat the M0 response-frequency baseline and the
implemented calibration protocol diverged from the configured protocol.

Do not overwrite or reuse these checkpoints with result schema version 4 or
later.
